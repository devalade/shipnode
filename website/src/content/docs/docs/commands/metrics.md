---
title: shipnode metrics
description: Open the PM2 monitoring dashboard.
---

```bash
npx shipnode metrics
```

Streams the PM2 `monit` view — per-process CPU and memory.

## Options

| Flag | Purpose |
|---|---|
| `--config <path>` | Use a non-default config file. |
